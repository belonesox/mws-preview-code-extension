- Нехватка стилей
  - начать с definition lists


- Copy-paste сложных объектов
  - Начать с Excel-таблиц

`mws push` имеет опцию `--summary`, типа комментарий к правке.
Сейчас опция `--summary` тупо дописывается в `syncCurrent`, а надо, чтобы значение summary подставлялось в `${summary}`  шаблона mws.sync.command.

Если что непонятно — спроси.